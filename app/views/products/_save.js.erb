$("ul.errors").html("");
<% if @product.errors.any? %>
  <% @product.errors.full_messages.each do |message| %>
    $("#errors").append($("<li />").html("<%= message.html_safe %>"));
  <% end %>
<% else %>
  $("#product-modal").modal("hide");
  $('#products_table').append("<%= j render 'index', product: @product %>");

<% end %>
